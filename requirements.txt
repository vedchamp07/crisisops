# Requirements for HF Spaces (demo/environment only)
# For local training, see requirements_train.txt
#
# Gradio 5.50+ works with current huggingface_hub (no legacy HfFolder / oauth).
# Gradio 4.44 on Spaces was failing with Jinja2/Starlette template cache errors;
# 5.50+ fixes the UI stack. Do not pin Jinja2 here — use Gradio’s dependencies.

gradio==5.50.0
huggingface_hub>=0.25.0
numpy>=1.26.0
openai>=1.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
matplotlib>=3.7.0
scipy>=1.11.0
seaborn>=0.13.0
datasets>=2.14.0
