# Requirements for HF Spaces (demo/environment only)
# For local training, see requirements_train.txt
#
# Gradio 4.44 (oauth) imports HfFolder from huggingface_hub; it was removed in 0.20+.
# Without this pin, pip pulls a current hub and the Space fails at "import gradio".
huggingface_hub>=0.19.0,<0.20.0

gradio==4.44.0
numpy>=1.26.0
openai>=1.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
matplotlib>=3.7.0
scipy>=1.11.0
seaborn>=0.13.0
datasets>=2.14.0
